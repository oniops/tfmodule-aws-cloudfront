/.idea/
.terraform.lock.hcl
**/.terraform/
*.tfstate.lock.info
**/docker/nginx.conf
/logs/
/dist/
/target/
/*.sh
**/*.log
**/*.tfstate.backup

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*-override.tf
*_override.tf.json
